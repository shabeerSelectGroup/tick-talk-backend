# Included from http {} via /etc/nginx/conf.d/.
# Photo-library originals are often 2–20MB. Nginx defaults to 1MB, which
# returns 413 without CORS headers. The app then shows "Unable to connect".
client_max_body_size 25m;
